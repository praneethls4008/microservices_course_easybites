apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loansdb-pvc
spec:
  accessModes:
    - ReadWriteOnce # Mounted by single pod
  resources:
    requests:
      storage: "700Mi" # Storage size