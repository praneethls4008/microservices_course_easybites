apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: accountsdb-pvc
spec:
  accessModes:
    - ReadWriteOnce # Mounted by single pod
  resources:
    requests:
      storage: "700Mi" # Storage size